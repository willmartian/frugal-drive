{
  "name": "frugal-drive",
  "version": "0.0.1",
  "type": "module",
  "bin": "./src/index.js",
  "scripts": {
    "build": "nx run cli:build",
    "watch": "nx run cli:build --watch",
    "cli": "node ../../dist/libs/cli/src/index.js"
  }
}
